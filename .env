# ── Cloudflare Worker (SaaS backend — auth, leads, payments, email/WA sending) ──
VITE_API_BASE_URL=https://lead-engine-backend.mukabiken.workers.dev

# ── Autonomous AI Agent Server (FastAPI on Railway/fly.io) ───────────────────
# Set this to your deployed FastAPI URL after deploying LeadForge-Autonomous-FIXED
VITE_AGENT_API_URL=https://leadforge-production-68d6.up.railway.app

# ── Agent Server Security Key ─────────────────────────────────────────────────
# A shared secret that protects the FastAPI routes from public access.
# Generate one: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# Set the SAME value as AGENT_API_KEY in your Railway environment variables.
VITE_AGENT_API_KEY=

# ── Google OAuth ──────────────────────────────────────────────────────────────
VITE_GOOGLE_CLIENT_ID=935512481664-hr7sos6eg42299og0ifegtg9e5ekn3tg.apps.googleusercontent.com
